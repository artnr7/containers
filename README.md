# containers

# Сборка тестов
1. Ручками:
```bash
mkdir build
cd build
cmake ..
make
```
И просто запускаете тесты вручную из папки `bin/tests/`

2. Автоматически(скриптом):
```bash
bash build_and_run.sh
```
